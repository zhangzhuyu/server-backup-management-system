<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ly.cloud.auth.mapper.SysRoleMenuMapper">

	<resultMap type="com.ly.cloud.auth.po.SysRoleMenu" id="SysRoleMenuResult">
		<result property="role_id"     column="role_id"      />
		<result property="menu_id"     column="menu_id"      />
	</resultMap>
	
	<select id="checkMenuExistRole" resultType="Integer">
	    select count(1) from ly_sys_role_menu where menu_id = #{menu_id}
	</select>

	<delete id="deleteRoleMenuByrole_id" parameterType="Long">
		delete from ly_sys_role_menu where role_id=#{role_id}
	</delete>
	
	<delete id="deleteRoleMenu" parameterType="Long">
 		delete from ly_sys_role_menu where role_id in
 		<foreach collection="array" item="role_id" open="(" separator="," close=")">
 			#{role_id}
        </foreach> 
 	</delete>
	
	<insert id="batchRoleMenu">
		insert into ly_sys_role_menu(role_id, menu_id) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.role_id},#{item.menu_id})
		</foreach>
	</insert>
	
</mapper> 